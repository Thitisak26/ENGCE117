#include <stdio.h>

    int main()
    {
        int i ;
        int n ;
            printf("Number : ");
            scanf("%d", &n );
            for (i = 1; i <= n; i++)
            {
                printf(" [%d] " "Hello word\n" , i);
            }          
        return 0;
    }